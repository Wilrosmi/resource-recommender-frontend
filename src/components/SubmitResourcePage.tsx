import {IState} from "../utils/types";

interface IProps {
  state: IState
}

function SubmitResourcePage({state}: IProps): JSX.Element {
  return (
    <>
      
    </>
  );
}

export default SubmitResourcePage;